name: Release apihub-build-config-diff

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version (e.g. v1.0.0)
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Normalize release version
        id: version
        shell: bash
        run: |
          VERSION="${{ inputs.version }}"
          VERSION="${VERSION#v}"
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid semver: ${{ inputs.version }}" >&2
            exit 1
          fi
          echo "tag=apihub-build-config-diff/v${VERSION}" >> "$GITHUB_OUTPUT"
          echo "name=v${VERSION}" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-go@v4
        with:
          go-version: '1.23'

      - name: Test
        run: cd ./apihub-build-config-diff && go test -v ./...

      - name: Build Linux
        run: cd ./apihub-build-config-diff && go build -v -o apihub-build-config-diff .
        env:
          GOOS: linux
          GOARCH: amd64

      - name: Build Windows
        run: cd ./apihub-build-config-diff && go build -v -o apihub-build-config-diff.exe .
        env:
          GOOS: windows
          GOARCH: amd64

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: apihub-build-config-diff ${{ steps.version.outputs.name }}
          generate_release_notes: true
          files: |
            apihub-build-config-diff/apihub-build-config-diff
            apihub-build-config-diff/apihub-build-config-diff.exe
